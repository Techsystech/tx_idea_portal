<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_idea_convert_task_wizard_form" model="ir.ui.view">
        <field name="name">idea.convert.task.wizard.form</field>
        <field name="model">idea.convert.task.wizard</field>
        <field name="arch" type="xml">
            <form string="Convert to Task">
                <group>
                    <field name="idea_id" readonly="1"/>
                    <field name="project_id"/>
                </group>
                <footer>
                    <button name="action_convert" string="Convert" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_idea_convert_task_wizard" model="ir.actions.act_window">
        <field name="name">Convert to Task</field>
        <field name="res_model">idea.convert.task.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="binding_model_id" ref="model_idea_request"/>
    </record>
</odoo>
